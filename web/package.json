{
  "name": "library-tracker-next",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "predev": "node scripts/ensure-node.mjs",
    "dev": "next dev -p 3000",
    "dev:reset": "rm -rf .next && npm run dev",
    "rebuild:sqlite": "npm rebuild better-sqlite3",
    "prebuild": "node scripts/ensure-node.mjs",
    "build": "next build",
    "start": "next start -p 3000",
    "lint": "next lint"
  },
  "dependencies": {
    "better-sqlite3": "^12.4.1",
    "next": "^15.1.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.0.2",
    "@types/react-dom": "^19.0.2",
    "typescript": "^5.7.2"
  }
}
